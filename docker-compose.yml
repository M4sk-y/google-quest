services:
  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: quest
      POSTGRES_PASSWORD: questpass
      POSTGRES_DB: google_quest
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
